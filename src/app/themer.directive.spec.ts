import { ThemerDirective } from './themer.directive';

describe('ThemerDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemerDirective();
    expect(directive).toBeTruthy();
  });
});
